datasources:
  default:
    url: ${JDBC_URL:`jdbc:h2:file:/Users/noefarese/Documents/CSS/Tonno/micronaut-datenbank/target/:lernjournal_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE`}
    username: ${JDBC_USER:sa}
    password: ${JDBC_PASSWORD:""}
    driverClassName: ${JDBC_DRIVER:org.h2.Driver}

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop

name: Docker Compose Build and Test

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Docker Compose
        run: docker-compose --version

      - name: Build and Start Backend and Frontend
        run: |
          docker-compose -f documents/css/tonno/lernjournal_projekt/docker-compose.yml up --build -d
          docker-compose -f documents/css/tonno/lernjournal_projekt/docker-compose.yml ps

      - name: Run Tests
        run: |
          cd frontend
          npm install
          npm test
          
          cd ..
          
          cd backend
          ./mvnw test
          
          cd ..

      - name: Stop Docker Compose
        run: |
          docker-compose -f  docker-compose -f documents/css/tonno/lernjournal_projekt/docker-compose.yml stop
          docker-compose -f  docker-compose -f documents/css/tonno/lernjournal_projekt/docker-compose.yml down